node_modules/
dist/
*.local
.env
.env.*
!.env.example

# The demo recording: kept on disk, uploaded to a video host, not committed.
demo/

# Demo videos kept on disk, linked not committed
demo.mp4
demo_x.mp4

# Internal pre-submission review notes (judge simulation) - not part of the deliverable
REVIEW_*.md

# Local burner secret key used only when re-recording the demo — never commit.
burner.b58
*.b58
__pycache__/
